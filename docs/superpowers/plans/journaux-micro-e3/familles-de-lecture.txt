Note de lecture des journaux du bloc E3 — RELEVÉE PAR LA COMMANDE le 21 août
2026, APRÈS la recette VM, sur les 65 fichiers de ce répertoire.

    fichiers                        : 65
    portant des séquences ANSI      :  6   (les journaux d'agent BRUTS)
    portant des CRLF                : 24   (ils viennent de la VM ; sans effet)
    portant un octet NUL            :  0
    non décodables en UTF-8         :  0

DEUX FAMILLES DE LECTURE :

| Famille | Compte | Ce qu'il faut faire |
| --- | --- | --- |
| les `agent-*.log` BRUTS | 6 | `sed 's/\x1b\[[0-9;]*m//g'` — ou lire le jumeau `-plat`, versé pour CHACUN des six |
| tout le reste (`*-plat.log`, `pilote-*.{json,log}`, `juge-*.log`, `r[0-5]*.log`, `instrument/`) | 59 | rien : ils se `grep`ent à plat |

✅ **AUCUN octet NUL nulle part** : contrairement à D10 et au presse-papier P1,
`grep -a` n'est nécessaire sur aucun fichier d'ici. ⚠️ *L'employer quand même ne
coûte rien, et une sortie VIDE n'est pas un zéro — c'est le piège qu'il évite.*

⚠️ **Les 24 CRLF viennent de la VM** (journaux d'agent bruts, leurs jumeaux
`-plat`, et le journal de la sonde de périphérique par défaut). Ils ne gênent
aucun `grep`.

🔵 **CE RÉPERTOIRE PORTE DEUX SORTES DE PIÈCES, ET LES DEUX COMPTENT** :

  - les SEPT journaux de ROUGE (`r0-harnais.log` à `r5-*.log`) et le harnais,
    joués sur l'HÔTE, sans VM ;
  - la RECETTE sur la VM : deux exécutions retenues (`1`, `2`), une d'injection
    de faute (`faute`), et **CINQ exécutions d'essai versées AVEC LEUR
    DIAGNOSTIC** (`essai-1` à `essai-5`) — ce sont les pièces des quatre défauts
    d'instrument que seule l'exécution pouvait trouver. Les effacer laisserait
    croire que le montage a marché du premier coup.

⛔ **CE QUE CE RÉPERTOIRE NE CONTIENT PAS, et c'est déclaré** : rien de la sonde
S1 (la salle émulée, l'AEC de Chrome). Elle exige de toucher le graphe audio
PipeWire de l'utilisateur de la machine hôte, et **ce consentement n'a pas été
donné**. Aucune configuration audio de l'hôte n'a été modifiée.
